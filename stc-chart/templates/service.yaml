apiVersion: v1
kind: Service
metadata:
  name: {{ include "stc-chart.fullname" . }}
  labels:
    {{- include "stc-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stc-chart.selectorLabels" . | nindent 4 }}
